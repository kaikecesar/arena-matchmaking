# Allow ESLint 10 while peer deps (eslint-plugin-react, eslint-plugin-import) catch up.
# Avoid downgrading eslint on every npm install.
legacy-peer-deps=true

# New installs go into package.json without ^ (exact versions).
save-exact=true
